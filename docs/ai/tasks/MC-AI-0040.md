# MC-AI-0040 — Establish deterministic snapshot test and benchmark foundation

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0040 |
| Level | basic |
| Parent | MC-AI-0032 |
| Status | done |
| Finalized | false |
| Priority | P1 |
| Complexity | high |
| Created | 2026-09-09 |
| Resolved | 2026-09-10 |
| Route | terra |
| Milestone | ED-010-S3 |
| Kind | chore |
| Baseline commit | — |
| Motivation | Create the durable automated acceptance base requested for current and future snapshots instead of one-off manual checks. |
| Pre-existing context | Snapshot 3 cross-cutting foundation. Tests must each protect a stable observable contract. The initial benchmark is RENDERER-ONLY submit/present-request throughput with constant player state and no server, network or simulation; it is neither full-game FPS nor actual displayed-present cadence. S7 owns the real full-game benchmark. |
| Acceptance | A reusable test harness provides safe ENet lifecycle, bounded scenario execution, structured smoke evidence, renderer lifecycle hooks, and reproducible CPU/frame benchmark metadata for later snapshots. Snapshot 3 renderer measurements are labeled RENDERER-ONLY submit/present-request throughput, use constant player state with no server/network/simulation, expose independent request/completion counters where available, and make no full-game or displayed-FPS claim. |
| Evidence | The integrated harness passed 29 focused acceptance checks and the full 204-test local CTest suite. The three real-ENet scenario contracts now also pass 20 consecutive repetitions each after removing a nondeterministic pre-scenario worker-tick assumption. Android compile/package evidence covers the exact imported harness payload, while final promoted-artifact Android runtime remains separate. |
| Blocker | — |
| Owner | Codex |

## Plan

- Repair the existing test network lifecycle race and expose bounded readiness/teardown.
- Add deterministic scenario and renderer smoke seams without requiring an interactive prompt.
- Add benchmark fixtures and documented result metadata; initially record baselines rather than hardcoding unknown device budgets.

## Children

- None

## Product changes

- Snapshot automation can exercise deterministic real-ENet scenarios and renderer-only capture/benchmark paths with bounded machine-readable evidence; it does not claim full-game or displayed FPS.

## Code changes

- Added bounded acceptance scenario execution, JSON/PPM evidence, renderer lifecycle capture and renderer-only benchmark tooling with explicit server teardown.

## Resolution

- Changes: Integrated the bounded deterministic scenario, renderer capture, structured JSON/PPM evidence, watchdog, lifecycle, and renderer-only benchmark foundations for reuse by later snapshots.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0040
