# MC-AI-0051 — Implement validated cached Runtime graphics kernels

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0051 |
| Level | basic |
| Parent | MC-AI-0035 |
| Status | done |
| Finalized | false |
| Priority | P1 |
| Complexity | high |
| Created | 2026-09-09 |
| Resolved | 2026-09-11 |
| Route | sol |
| Milestone | ED-010-S4 |
| Kind | feature |
| Baseline commit | — |
| Motivation | Give Runtime graphics a stable SPIR-V program boundary instead of embedding shader-module and pipeline policy in the game renderer. |
| Pre-existing context | This S4 slice establishes graphics-kernel use only. Standalone compute execution is MC-AI-0054; high-level source compilation and Script-to-kernel lowering are future work. |
| Acceptance | CoreCpp Runtime Kernel accepts SPIR-V bytes and files, validates modules and reflected entrypoint/binding contracts, caches modules/programs and builds the graphics pipelines needed by RuntimeGraphics. Cache keys and destruction include device/program identity, errors are deterministic and diagnostic, packaged modules are capability-compatible, and Client shader use crosses only the Kernel/Graphics interfaces. Focused malformed-module, reflection, cache-hit/invalidation and device-recreation tests pass; no source compiler, Script lowering or standalone compute claim is made. |
| Evidence | Exact clean CoreCpp 394cc5e3724643fd5d2cb3284230fbd7236ee144 and CoreProject2026 eb2b1a52c257e3a86b6ee12ada247c38e619e8bc desktop packages configured fresh with exact provenance. Focused fresh CoreCpp malformed-SPIR-V, reflection, program contract, cache identity, GPU pipeline cache, device-lifetime and invalidation tests passed 12/12. MinecraftClone desktop build and CTest passed 153/153, including Client program diagnostics and hot-reload/recreation smoke coverage; a validation-enabled foreground RendererSmokeTest passed 2/2. A fresh MC\_BUILD\_CLIENT=OFF server-only build and CTest passed 118/118. Fresh Android NDK 27 API 28 arm64-v8a linked libmc\_android.so against the exact Android packages; only upstream native\_app\_glue warnings were emitted. |
| Blocker | — |
| Owner | Codex MC-AI-0051 worker |

## Plan

- Specify immutable SPIR-V program/reflection descriptors and validated diagnostics for the required graphics stages.
- Implement device-aware module and pipeline caching with explicit lifetimes and deterministic invalidation.
- Route Client graphics programs through Kernel and cover malformed inputs, entrypoints, bindings, capability mismatch and recreation.

## Children

- None

## Product changes

- None

## Code changes

- Replaced Client-owned raw shader-module and graphics-pipeline creation/destruction with the installed CoreCpp GraphicsProgram and VulkanKernelCache seam on the presentation scope's current device.
- Kept Client grid/player push-constant layouts, dynamic-rendering target format and scene draw order; reversed fallback quad index winding to preserve visible output with the generic cached pipeline.
- Added program-contract diagnostics and hot-reload/recreation smoke coverage, while retaining a graphics-free server-only dependency closure.

## Resolution

- Changes: Client grid and player fallback programs now validate through CoreCpp GraphicsProgram and acquire cached shader modules and dynamic-rendering pipelines through VulkanKernelCache using the current PresentationResourceScope device identity. Client retains only game push-constant layouts, draw order and target format; recreation invalidates the old device cache before layout/scope release. Fallback triangle winding matches the Kernel cache's generic back-face-culling pipeline without changing displayed geometry.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0051
