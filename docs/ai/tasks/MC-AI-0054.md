# MC-AI-0054 — Execute standalone Runtime compute kernels

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0054 |
| Level | basic |
| Parent | MC-AI-0036 |
| Status | done |
| Finalized | true |
| Priority | P1 |
| Complexity | high |
| Created | 2026-09-09 |
| Resolved | 2026-09-13 |
| Route | sol |
| Milestone | ED-010-S5 |
| Kind | feature |
| Baseline commit | — |
| Motivation | Prove Kernel is a reusable computation service as well as a graphics implementation detail before later generation and GPU-driven work. |
| Pre-existing context | Build on the S4 SPIR-V validation/reflection/cache contracts. This is bounded compute execution from supplied SPIR-V, not a shader source compiler or Script-to-kernel compiler. |
| Acceptance | CoreCpp Runtime Kernel creates and dispatches a validated compute pipeline from both SPIR-V bytes and file input, checks reflected bindings and ranges, synchronizes bounded buffers correctly, returns deterministic diagnostics, and produces a verified output buffer without initializing presentation or a game client. Device/program-aware cache reuse and invalidation tests pass, including malformed inputs and cancellation/error cleanup; no high-level compiler/JIT claim is made. |
| Evidence | CoreCpp 1a1d4095bd553ed1ea382523eecd22c9748bbe9f publishes the standalone Runtime compute dependency. 186/188 initial CoreCpp checks passed; two package-provenance checks passed after clean exact-source reconfigure/install. Real Vulkan dispatch transformed {0,1,2,9} to {1,4,7,28} with validation, no presentation, file/byte program caching, and bounded resource checks. Exact clean dependency published and pinned; /private/tmp/mc-s5-compute-review.txt records approved review. |
| Blocker | — |
| Owner | — |

## Plan

- Extend Kernel descriptors and reflection validation only for compute resources needed by the acceptance fixture.
- Implement bounded buffer binding, dispatch, synchronization/readback and cache lifecycle independent of presentation.
- Verify real output data, file/byte parity, invalid bindings, cleanup and warmed cache behavior.

## Children

- None

## Product changes

- None

## Code changes

- None

## Resolution

- Changes: Delivered and consumed the reviewed standalone CoreCpp compute pipeline and exact clean package; no graphics window or game client is required for compute dispatch.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0054
