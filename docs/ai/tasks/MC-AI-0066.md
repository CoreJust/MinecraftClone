# MC-AI-0066 — Verify Vulkan surface and suboptimal-acquire correctness

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0066 |
| Level | basic |
| Parent | MC-AI-0032 |
| Status | done |
| Finalized | false |
| Priority | P0 |
| Complexity | high |
| Created | 2026-09-09 |
| Resolved | 2026-09-10 |
| Route | terra |
| Milestone | ED-010-S3 |
| Kind | bug |
| Baseline commit | — |
| Motivation | Prevent Snapshot 3 from shipping a macOS surface ownership defect or continuing a frame with an image index invalidated by swapchain recreation. |
| Pre-existing context | Focused audit hypotheses require runtime verification before code changes: the manually created CAMetalLayer may need an explicit delegate=view, although NSView may wire it automatically; VK\_SUBOPTIMAL\_KHR on image acquisition currently appears to trigger synchronous reload through VK\_CHECK and then continue with the pre-reload image index. Reproduce against the exact current overlay and change only confirmed defects. |
| Acceptance | On the exact current macOS/Vulkan overlay, a focused probe records whether the attached CAMetalLayer has the required view/delegate ownership and changes it only if runtime evidence confirms a defect. Acquire handling never submits or presents an image index from a swapchain generation invalidated by OUT\_OF\_DATE/SUBOPTIMAL recreation; the frame is safely retried or skipped under an explicit contract. Focused lifecycle/acquire regressions plus validation-enabled resize/reload/close runtime acceptance pass, and any unconfirmed hypothesis is documented rather than converted into speculative code. |
| Evidence | LLDB confirmed the CAMetalLayer delegate defect; lifecycle regressions prove stale SUBOPTIMAL frames neither submit nor present and the next frame succeeds. Validation-enabled native smoke passed all four cases, including resize/reload/close and no validation errors. |
| Blocker | — |
| Owner | presentation\_budget\_design |

## Plan

- Instrument and reproduce the current CAMetalLayer attachment/delegate behavior and suboptimal acquire/reload path before selecting a fix.
- Repair only confirmed surface ownership and swapchain-generation violations with an explicit skip/retry result instead of continuing stale frame state.
- Add focused acquire-generation/lifecycle regression coverage and rerun validation-enabled macOS resize, reload and close acceptance.

## Children

- None

## Product changes

- None

## Code changes

- None

## Resolution

- Changes: Closed the confirmed macOS surface and acquire-lifecycle repair; final release artifact acceptance remains aggregate-owned.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0066
