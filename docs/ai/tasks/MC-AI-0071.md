# MC-AI-0071 — Validate Android package evidence against the APK native ABI set and required native library

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0071 |
| Level | basic |
| Parent | MC-AI-0032 |
| Status | active |
| Finalized | false |
| Priority | P1 |
| Complexity | medium |
| Created | 2026-09-10 |
| Resolved | — |
| Route | luna |
| Milestone | ED-010-S3 |
| Kind | bug |
| Baseline commit | f10d18c80fa1cfa7d1a4c5e4b21d9fa97b429754 |
| Motivation | Release evidence previously trusted the caller-provided ABI and could falsely label an x86\_64-only APK as arm64-v8a. |
| Pre-existing context | Snapshot 3 release-evidence correctness follow-up from the candidate publication review. Validate only the supported APK native-library contract; do not extract, execute, or generalize into an APK scanner. |
| Acceptance | Android package evidence is written only when the APK native-library ABI set exactly matches every declared --abi, each lib/&lt;abi&gt;/ path agrees with a bounded ELF-header architecture check, and every declared ABI contains libmc\_android.so. Caller mismatch, directory/header mismatch, malformed or missing native payload, and undeclared extra ABI fixtures fail closed without writing evidence; a valid arm64-v8a APK shape remains accepted. |
| Evidence | The Android evidence helper now reads at most 64 header bytes from each canonical direct lib/&lt;abi&gt;/\*.so APK entry, validates little-endian ELF class, machine, and header size, rejects normalized duplicate paths, requires directory/header ABI agreement, requires the packaged ABI set to equal --abi, and requires libmc\_android.so for every declared ABI. Twenty focused package tests pass, covering a valid arm64-v8a shape plus caller mismatch, directory/header mismatch, malformed and missing native payloads, an undeclared second ABI, and normalized duplicate paths. All 132 script tests pass. The helper also accepted the existing 5aa9cd370314be205a07e0d96e2d5eb2713ad14c19fec408658ee33a4e3e8603 development APK as arm64-v8a; this validates package shape only and is not final release-artifact evidence. |
| Blocker | — |
| Owner | s3\_integration |

## Plan

- Clarify acceptance and implementation steps.
- Inspect bounded ELF headers for direct native-library entries and derive their Android ABIs.
- Require exact declared/package ABI equality and libmc_android.so for each declared ABI.
- Cover supported arm64-v8a evidence and fail-closed mismatch, malformed, missing, and multi-ABI cases.

## Children

- None

## Product changes

- Android package evidence now binds its claimed ABI set to the APK native payload.

## Code changes

- script/package_snapshot.py
- script/tests/test_package_snapshot.py

## Resolution

- Changes: —

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0071
