# MC-AI-0072 — Repair GitHub Actions runner-context cache setup

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0072 |
| Level | basic |
| Parent | MC-AI-0032 |
| Status | done |
| Finalized | false |
| Priority | P1 |
| Complexity | low |
| Created | 2026-09-10 |
| Resolved | 2026-09-10 |
| Route | luna |
| Milestone | ED-010-S3 |
| Kind | bug |
| Baseline commit | — |
| Motivation | Hosted S3 validation rejects runner.temp in both workflows before job scheduling, blocking the intended macOS and Windows CI paths. |
| Pre-existing context | Diagnosis /private/tmp/mc-s3-x3-ci-validation-diagnosis.txt at candidate 9ae5c609945384fae6e12474fdbf2034c184c52d7 identifies the exact GitHub static-validation error on ai-checks.yml line 46 and snapshot-artifacts.yml line 69. Scope is limited to workflow context placement, cache-directory setup, and a cheap static regression guard; do not broaden CI, rerun hosted workflows, or publish. |
| Acceptance | Both S3 GitHub workflows pass static validation without using runner.temp at job-level env scope; macOS and Windows configure VCPKG\_DEFAULT\_BINARY\_CACHE at step runtime, create the cache directory on the corresponding runner, and preserve existing build/package behavior. A focused static regression test rejects runner-context expressions in job-level env and verifies the platform-specific runtime setup. Hosted GitHub success remains unclaimed. |
| Evidence | Both workflows now configure the vcpkg cache from platform-specific runtime steps: macOS creates $RUNNER\_TEMP/vcpkg-binary-cache with mkdir -p and exports it through GITHUB\_ENV; Windows creates the equivalent %RUNNER\_TEMP% path and exports it through GITHUB\_ENV. The focused workflow regression passes, both workflow files parse with Ruby YAML, all 133 script tests pass, and ai\_check.py --fast passes. Hosted GitHub validation and desktop build execution remain unclaimed. |
| Blocker | — |
| Owner | s3\_finalization |

## Plan

- Inspect both workflow job/step contexts and confirm the runner-context validation failure.
- Move cache configuration into platform-specific runtime steps that create the directory and export GITHUB_ENV.
- Add a focused static workflow regression test covering all repository workflows and both affected runtime setups.
- Run focused tests and ai_check.py --fast, obtain exact staged Luna/high review, then commit with one Task-ID trailer.

## Children

- None

## Product changes

- None

## Code changes

- .github/workflows/ai-checks.yml
- .github/workflows/snapshot-artifacts.yml
- script/tests/test_workflow_contexts.py

## Resolution

- Changes: Recorded the X4 runner-context cache repair as locally accepted: both workflows now configure platform-specific vcpkg cache directories at step runtime, and the focused static guard prevents job-level runner context use. Hosted workflow and desktop build success remain pending.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0072
