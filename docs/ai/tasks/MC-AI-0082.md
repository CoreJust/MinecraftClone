# MC-AI-0082 — Parse multi-architecture macOS dylib metadata correctly

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0082 |
| Level | basic |
| Parent | MC-AI-0032 |
| Status | done |
| Finalized | false |
| Priority | P0 |
| Complexity | medium |
| Created | 2026-09-10 |
| Resolved | 2026-09-10 |
| Route | luna |
| Milestone | ED-010-S3 |
| Kind | bug |
| Baseline commit | — |
| Motivation | X9 passed all 204 macOS tests but packaging misclassified the repeated arm64 otool section header for the universal Vulkan loader as a non-relocatable dependency. |
| Pre-existing context | The Vulkan loader and MoltenVK inputs already have relocatable @rpath install names; this repair keeps the external-dependency gate fail-closed rather than waiving or rewriting it. |
| Acceptance | macOS package validation parses every architecture section from fat-binary otool output without treating repeated section headers as dependencies, proves each dylib install name with otool -D resolves to its packaged path, and still rejects true absolute or unpackaged dependencies. |
| Evidence | All 24 focused package tests pass, including fat-header parsing, install-name separation, mismatched install-name rejection, and preserved absolute dependency rejection. Real validation passes for the current mc\_main plus the universal Vulkan loader and MoltenVK SDK dylibs; their otool -D names resolve to the packaged lib paths. |
| Blocker | — |
| Owner | — |

## Plan

- Parse only exact thin or supported-architecture otool section headers as headers.
- Use otool -D to distinguish and validate each dylib's own install name.
- Retain fail-closed checks for true absolute and missing relative dependencies.

## Children

- None

## Product changes

- None

## Code changes

- docs/PACKAGING.md
- script/package_snapshot.py
- script/tests/test_package_snapshot.py

## Resolution

- Changes: Separated multi-architecture otool headers, dylib install names, and load dependencies; validated each install name against its archive location while preserving rejection of external dependencies.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0082
