# MC-AI-0098 — Make Windows release phase fail immediately

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0098 |
| Level | basic |
| Parent | MC-AI-0035 |
| Status | done |
| Finalized | false |
| Priority | P1 |
| Complexity | low |
| Created | 2026-09-11 |
| Resolved | 2026-09-11 |
| Route | luna |
| Milestone | ED-010-S4 |
| Kind | bug |
| Baseline commit | — |
| Motivation | The cmd release phase checks only selected errorlevels, so a failed Windows build or package command can be masked by a later successful command and produce misleading release evidence. |
| Pre-existing context | Discovered during the Snapshot 4 pre-CI audit. Scope is the existing Windows package command chain and its static/failure-injection regression; no workflow redesign or hosted-success claim. |
| Acceptance | The Windows desktop release command phase stops immediately after every configure, build, test, shader-validation, install, packaging, evidence, checksum, and private-source-exclusion failure; a focused failure-injection regression proves no later command can mask an earlier nonzero exit; successful packaging behavior remains unchanged. |
| Evidence | The Windows snapshot phase now places an immediate errorlevel guard after every fallible setup, configure, build, CTest/log-read, shader-validation, install, directory creation, packaging, evidence, checksum, and private-source-exclusion command. The focused test injects failure at each command and proves no successor runs; the successful path reaches all commands. python3 -m unittest script.tests.test\_build\_snapshot passed 16/16; python3 -m script.code\_checks and git diff --check passed. |
| Blocker | — |
| Owner | Luna Windows release worker |

## Plan

- Enumerate every fallible command in the Windows packaging phase and enforce immediate exit after each one.
- Add a focused regression that injects a failure before a successful command and proves the phase returns nonzero without executing the successor.

## Children

- None

## Product changes

- No product behavior changes; Windows release failures cannot be masked by later successful commands.

## Code changes

- Harden the Windows snapshot packaging cmd chain and add focused workflow regression coverage.

## Resolution

- Changes: Made the complete Windows release command chain fail immediately and added class-wide failure-injection regression coverage.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0098
