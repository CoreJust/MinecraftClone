# MC-AI-0108 — Make source policy test portable on Windows

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0108 |
| Level | basic |
| Parent | MC-AI-0035 |
| Status | done |
| Finalized | false |
| Priority | P0 |
| Complexity | low |
| Created | 2026-09-11 |
| Resolved | 2026-09-11 |
| Route | luna |
| Milestone | EarlyDev 0.1.0:4 |
| Kind | bug |
| Baseline commit | — |
| Motivation | Hosted snapshot artifacts run 34633199620 reached finalized source policy on Windows, where test\_private\_dependency\_fetch\_pins\_github\_host\_key raised IndexError because Path.chmod recorded mode as the first positional argument for a patched bound method. |
| Pre-existing context | macOS and Android artifact jobs passed at exact source 4cdbbe8; Windows did not reach its package build. Preserve the failed run and keep publication paused for exact candidate preview. |
| Acceptance | Make the GitHub known-host permission assertion independent of mock call storage and host argument binding; retain POSIX mode validation and Windows ACL production behavior; run the focused CI acquire suite plus the fast source-policy gate; obtain one exact-task Luna/high review; commit with Task-ID MC-AI-0108; rerun hosted Windows before claiming the candidate complete. |
| Evidence | Hosted snapshot artifacts run 34633199620 Windows job 103374865250 failed reproducibly before build: test\_private\_dependency\_fetch\_pins\_github\_host\_key indexed chmod\_call.call\_args.args\[1\], but patched Path.chmod recorded the single mode argument at args\[0\]. The focused CI acquire suite passes 21 tests and the full fast gate passes Python tests, existing debug build, and CTest. Hosted Windows replacement remains required before cross-platform candidate acceptance. |
| Blocker | — |
| Owner | — |

## Plan

- Correct only the faulty call assertion, add no behavior change to credential handling, run focused and fast gates, review the immutable staged tree, then promote for hosted rerun.

## Children

- None

## Product changes

- None; this repairs cross-platform verification of an existing security invariant.

## Code changes

- Assert the patched Path.chmod call with its actual bound-method call signature and preserve platform-specific mode checks.

## Resolution

- Changes: Made the Windows known-host permission test assert the patched bound Path.chmod call directly, preserving the production POSIX permission and Windows ACL behavior.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0108
