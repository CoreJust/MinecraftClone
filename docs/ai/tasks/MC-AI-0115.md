# MC-AI-0115 — Make hosted Windows private-dependency builds compiler-family consistent and fail fast

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0115 |
| Level | basic |
| Parent | MC-AI-0035 |
| Status | done |
| Finalized | true |
| Priority | P0 |
| Complexity | medium |
| Created | 2026-09-12 |
| Resolved | 2026-09-12 |
| Route | luna |
| Milestone | EarlyDev:Initiation 0.1.0:4 |
| Kind | chore |
| Baseline commit | 6ff94f45d72692d4fe3dd6e5c8bd863c56fe2853 |
| Motivation | Unblock exact Windows Snapshot 4 packaging after hosted evidence proved compiler-family contamination and false-green cmd phases. |
| Pre-existing context | Snapshot artifacts run 34659649762 job 103459345949 installed private dependencies before VsDevCmd, produced GNU .a archives, and then failed the MSVC link with 67 unresolved externals. AI checks run 34659649793 Windows jobs reported success even though their logs contain the same link failure because cmd phases did not propagate build, CTest, or shader command failures. Generated nested CMake scripts also embedded backslash paths that CMake parsed as invalid escapes. |
| Acceptance | On Windows, activate the VS 2022 x64 developer environment before installing private packages and keep that environment for the game build; fail immediately and preserve the original exit code for every private-install, configure, build, CTest, shader, install, package, checksum, and exclusion command in the affected cmd workflows; normalize generated CMake script path inputs so Windows backslashes cannot become escapes; add deterministic tests for compiler-family setup, complete phase failure propagation, and Windows path normalization; preserve macOS and Android behavior; obtain one Luna/high exact-staged review; require a new hosted Windows build/test/package pass before claiming the blocker fixed. |
| Evidence | Focused Windows workflow, path-normalization, and package tests passed: 47/47. python3 script/ai\_check.py --fast passed; git diff --check, ai\_tasks.py check, ai\_docs.py check, and ai\_plan.py check passed. The exact hosted failures remain recorded as snapshot-artifacts run 34659649762 job 103459345949 and false-green ai-checks run 34659649793. A fresh hosted Windows build/test/package pass is still required before Snapshot 4 may claim the blocker fixed. |
| Blocker | — |
| Owner | Luna implementation; Codex integration |

## Plan

- Clarify acceptance and implementation steps.
- Add deterministic workflow and generated-script regressions that reproduce the missing VS setup, false-green phases, and backslash escape boundary.
- Apply one shared Windows developer-environment entry boundary and normalize generated CMake path values without changing non-Windows behavior.
- Run focused tooling/tests, full task gate, Luna/high staged review, commit with Task-ID MC-AI-0115, then verify a fresh hosted Windows artifact job.

## Children

- None

## Product changes

- None

## Code changes

- Activate VS 2022 x64 before Windows private dependency package builds in both hosted workflows.
- Propagate each affected Windows CI phase failure and normalize path-valued CMake definitions.
- Cover MSVC setup ordering, phase guards, and backslash-path normalization with deterministic tests.

## Resolution

- Changes: Windows private dependency compilation and game compilation now both enter the VS 2022 x64 environment; affected cmd phases propagate their original nonzero status; CMake path-valued definitions and generated nested scripts normalize Windows separators.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0115
