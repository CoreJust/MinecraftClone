# MC-AI-0122 — Eliminate redundant AI workflow checks

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0122 |
| Level | basic |
| Parent | MC-AI-0035 |
| Status | done |
| Finalized | true |
| Priority | P0 |
| Complexity | medium |
| Created | 2026-09-12 |
| Resolved | 2026-09-12 |
| Route | luna |
| Milestone | EarlyDev 0.1.0:4 |
| Kind | chore |
| Baseline commit | — |
| Motivation | Snapshot delivery repeatedly paid for identical Python suites and complete release gates at adjacent commit and push boundaries. Current receipts fingerprint only script Python files even though the tests inspect repository inputs, while index and worktree identity hashes prevent safe content-equivalent reuse after staging or commit. |
| Pre-existing context | The user authorized one bounded local workflow repair before resuming Snapshot 4. This task owns scripts, repository hooks, focused tests, policy guidance, and task records only. It must not add a C++ dependency cache, global configuration, new infrastructure, or weaken final ai-main, tag, or release gates. |
| Acceptance | A repeated unchanged ordinary development workflow does not rerun the same full gate at both the caller and pre-push boundaries. The full Python tooling suite is reused only from a successful, well-formed receipt bound to every repository input the suite may read, the exact command and relevant environment; changed inputs, commands, environments, failures, and missing or malformed evidence all force execution. Equivalent governed content remains reusable across staging or commit identity changes. AI\_TASK does not leak into harness-test subprocesses. Ordinary ai-dev pushes run a proportionate development gate, while ai-main and tag or release paths retain complete strict release checks. Actual hook-entrypoint regressions cover allowed reuse and every invalidation class; build and CTest reuse remain disabled unless their full dependency closure is cheaply proven. |
| Evidence | Focused ai\_check, ai\_history, and pre-finalization candidate regressions passed 64/64 locally before review correction; the review correction then passed the affected ignored-discovery and full-suite-reuse regressions 2/2. Coverage includes stale receipt invalidation, full Python-suite reuse, command/environment/input/malformed-evidence invalidation, ignored discovered-test invalidation, AI\_TASK isolation, proportionate ai-dev hook routing, candidate-scoped Task-ID history, and owned temporary-repository Git maintenance settings. The hosted efficiency target remains unclaimed because the available 112s and 2s runs both failed and are not comparable successful evidence. |
| Blocker | — |
| Owner | Codex |

## Plan

- Bind Python-suite receipts to conservative real repository inputs, command, environment, result, and receipt schema while removing staging-only identity invalidation.
- Remove duplicate caller execution and make the ordinary ai-dev pre-push gate proportionate without weakening ai-main or release gates.
- Prevent task-selector leakage into test subprocesses and exercise the real hook entrypoints across success, failure, changed-input, command, environment, and missing-evidence cases.
- Measure a representative repeated workflow using existing timing output without claiming the target until comparable successful evidence exists.

## Children

- None

## Product changes

- None

## Code changes

- Reuse the full Python tooling suite only when a conservative content-bound receipt remains valid.
- Avoid duplicate complete checks in ordinary commit and ai-dev push workflows while preserving final release gates.
- Add focused receipt and hook-entrypoint regressions, task-selector isolation, and concise project workflow policy.
- Invalidate stale tooling receipts when environment or repository-input fingerprinting fails before phase execution.
- Scope aggregate Task-ID trailer validation to exact candidate-range commits and explicit promoted source parents, ignoring unrelated refs while preserving unknown-trailer failures in the candidate.
- Disable Git automatic maintenance and garbage collection in the owned pre-finalization temporary repository and guard the setting with a regression.
- Classify mixed tooling, policy, hook, and documentation changes as full-python so ordinary development checks stay proportionate without invoking C++ fallback work.

## Resolution

- Changes: Reused only content-, command-, environment-, result-, and evidence-bound tooling receipts; invalidated stale receipts on fingerprint failures; scoped aggregate history validation to exact candidate commits plus explicit promoted source parents; routed ordinary ai-dev pushes through fast checks while preserving strict release paths; and disabled Git background maintenance in the owned pre-finalization fixture to prevent teardown races.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0122
