# MC-AI-0129 — Invoke POSIX shell for Windows pre-push fixture tests

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0129 |
| Level | basic |
| Parent | MC-AI-0035 |
| Status | done |
| Finalized | true |
| Priority | P1 |
| Complexity | low |
| Created | 2026-09-12 |
| Resolved | 2026-09-12 |
| Route | luna |
| Milestone | EarlyDev 0.1.0:4 |
| Kind | chore |
| Baseline commit | — |
| Motivation | Hosted Windows test run 34694560321 and rerun 34694560310 fail solely because test\_ai\_dev\_pre\_push\_entrypoint\_runs\_fast\_gate\_and\_propagates\_failure executes the POSIX hook script directly and Windows raises WinError 193 before the hook runs. |
| Pre-existing context | The production .githooks/pre-push remains a POSIX shell script. The annotated-tag fixture already detects sh explicitly; the ai-dev success and failure subprocess calls are the remaining direct-script class. Preserve real hook input, fake Python argument logging, and failure propagation rather than skipping or weakening Windows assertions. |
| Acceptance | The ai-dev pre-push fixture tests invoke the real .githooks/pre-push entrypoint directly on POSIX and through an explicitly detected POSIX shell on Windows, preserving the exact fake-Python arguments and success/failure exit-code assertions; nearby direct-script subprocess calls are covered; production hook behavior is unchanged; focused tests, proportional Python validation, and one exact-staged Luna/high review pass; commit with Task-ID MC-AI-0129 and do not push. |
| Evidence | Focused python3 -m unittest script.tests.test\_ai\_check passed 35/35, including success, failure, exact argument, and Windows shell-selection regressions. python3 script/ai\_tasks.py check and git diff --check passed. python3 script/ai\_check.py --fast passed backlog, current-plan, diff-working, diff-cached, and the full unittest suite; its inherited docs phase remains failed because MC-AI-0035.md exceeds the existing 900-word limit and docs/code/source\_state.json is stale on the origin/ai-dev baseline. Hosted Windows rerun evidence is not claimed. |
| Blocker | — |
| Owner | Codex |

## Plan

- Clarify acceptance and implementation steps.
- Trace all script-test subprocess calls that execute repository hook files and identify the direct-script Windows failure class.
- Add a shared cross-platform hook runner that selects an explicitly detected POSIX shell on Windows while retaining direct POSIX execution.
- Use the runner for both ai-dev success and failure assertions, add focused command-selection regression coverage, and verify exact hook arguments and propagated exit status.
- Run focused tests and proportional Python checks, stage exact task scope, obtain a Luna/high staged review, record its receipt, and commit without pushing.

## Children

- None

## Product changes

- None; this is a test portability repair with no runtime hook behavior change.

## Code changes

- script/tests/test_ai_check.py now selects str(.githooks/pre-push) directly on POSIX and uses shutil.which("sh") with exec "$1" on Windows; the ai-dev success and failure assertions share that command path and verify the exact fake-Python arguments after both runs.

## Resolution

- Changes: Added a cross-platform test-only pre-push command helper that executes the real hook directly on POSIX and through an explicitly detected sh on Windows; both ai-dev success and failure cases preserve exact fake-Python arguments and failure propagation. No production hook behavior changed.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0129
