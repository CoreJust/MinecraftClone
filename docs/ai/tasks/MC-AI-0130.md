# MC-AI-0130 — Make Windows Debug and Release package/test configuration ABI-consistent and portable

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0130 |
| Level | basic |
| Parent | MC-AI-0035 |
| Status | done |
| Finalized | false |
| Priority | P2 |
| Complexity | medium |
| Created | 2026-09-12 |
| Resolved | 2026-09-12 |
| Route | luna |
| Milestone | EarlyDev 0.1.0:4 |
| Kind | chore |
| Baseline commit | — |
| Motivation | Make Windows Debug and Release package/test configuration ABI-consistent and portable |
| Pre-existing context | Snapshot 4 b519 logs show Windows Debug and Release package consumers linking Release-built private libraries: Debug fails with MSVC RuntimeLibrary/ITERATOR\_DEBUG\_LEVEL LNK2038, while Release fails the padding-sensitive MessageTest and the canonical TypeName test because CoreCpp returns a class-key prefix. The nested CoreCpp package consumer also hardcodes Debug and fails from the same ABI mismatch. MinecraftClone.ServerOnlyBuild is a separately registered CTest and independently reaches the 60-second default timeout; AcceptanceCli failure remains cascading until an aligned executable rerun. |
| Acceptance | Windows Debug and Release consumers build private CoreCpp and CoreProject2026 packages with the matching preset, nested package/server-only fixtures inherit the enclosing CMAKE\_BUILD\_TYPE, and MessageTest checks observable fields plus exact serialized bytes without object-padding assumptions. The separately registered server-only build has an explicit bounded platform timeout. Preserve the canonical TypeName test and document its failing CoreCpp package as a separate cross-repo blocker; do not weaken debug checks or claim hosted Windows success without a fresh rerun. |
| Evidence | Focused CI acquisition/workflow suites passed 35 tests; MessageTest passed 6 tests; local CTest passed 129 tests including the CoreCpp package consumer and MinecraftClone server-only build; python3 script/ai\_check.py --fast --require-index-match passed. Hosted Windows rerun remains the snapshot gate after the separately owned CoreCpp CC-0029 canonical type-name fix and exact repin. |
| Blocker | — |
| Owner | Codex |

## Plan

- Carry the consuming debug/release preset into private dependency package builds in both desktop workflows without disabling debug checks.
- Pass the enclosing CMAKE_BUILD_TYPE into nested CoreCpp and server-only consumer fixtures and add deterministic configuration regressions.
- Give the separately registered server-only build a bounded platform-specific timeout and assert its registration contract.
- Replace MessageTest object memcmp with field and serialized-byte assertions; retain the canonical TypeName expectation and report the CoreCpp fix separately.
- Run focused tests and python3 script/ai_check.py --fast --require-index-match, recording the inherited docs/source-state gate and hosted Windows rerun requirement.

## Children

- None

## Product changes

- None

## Code changes

- .github/workflows/ai-checks.yml
- .github/workflows/snapshot-artifacts.yml
- script/ci/acquire.py
- script/tests/test_ci_acquire.py
- script/tests/test_workflow_contexts.py
- tests/cmake/corecpp_server_consumer_test.cmake.in
- tests/cmake/minecraftclone_server_only_test.cmake.in
- tests/CMakeLists.txt
- tests/core/shared_message_tests.cpp

## Resolution

- Changes: Made private dependency installs match the consuming Debug or Release preset, propagated the enclosing build type into nested consumers, replaced padding-sensitive message comparisons with field and wire-byte checks, and assigned bounded platform-specific server-only build timeouts.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0130
