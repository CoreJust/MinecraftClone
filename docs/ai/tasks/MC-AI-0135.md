# MC-AI-0135 — Repair nested Windows Release consumers and retain failed CTest diagnostics

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0135 |
| Level | basic |
| Parent | MC-AI-0035 |
| Status | done |
| Finalized | true |
| Priority | P0 |
| Complexity | medium |
| Created | 2026-09-12 |
| Resolved | 2026-09-12 |
| Route | luna |
| Milestone | EarlyDev 0.1.0:4 |
| Kind | bug |
| Baseline commit | — |
| Motivation | Hosted Windows release evidence shows the nested consumers configure against Release packages yet build their default Debug configuration, yielding LNK2038 RuntimeLibrary and iterator-debug mismatches; the snapshot workflow returns before printing the redirected CTest log. |
| Pre-existing context | Hosted ai-checks Windows Release run 34703157457 reports 219/222 passing: CoreCpp.ServerOnlyPackageConsumer and MinecraftClone.ServerOnlyBuild link Release CoreCpp libraries into Debug nested targets. The parent CMake preset is single-config Ninja Release, but nested CMake invocation has no generator and Windows selects a multi-config Visual Studio generator, where CMAKE\_BUILD\_TYPE does not select the build or CTest configuration. AcceptanceCli.RuntimeModes has no direct executable diagnostic in the supplied trace and may be cascading. |
| Acceptance | Nested CoreCpp package and MinecraftClone server-only consumers select the enclosing Debug or Release configuration when their nested generator is multi-config, while single-config nested builds retain their existing behavior. The Windows snapshot workflow prints its redirected CTest log before returning CTests exact nonzero exit status. The existing AcceptanceCli failure is changed only with independent evidence. Focused regressions verify the generated nested configuration and failure-log ordering; a fresh hosted Windows rerun remains required. |
| Evidence | Focused static workflow/build and AcceptanceCli failure-path tests passed 31/31. The new runtime test invokes a real failing child launcher and verifies the hosted diagnostic includes the exact command, exit code, stdout, and stderr. Fresh Ninja Release configure/build with VCPKG\_MANIFEST\_INSTALL=OFF passed CoreCpp.ServerOnlyPackageConsumer, MinecraftClone.ServerOnlyBuild, and AcceptanceCli.RuntimeModes \(3/3\); generated CTest commands resolve MC\_TEST\_CONFIGURATION=Release. Re-running the two nested fixtures with CMAKE\_GENERATOR=Ninja Multi-Config passed 2/2, exercising the multi-config cache/configuration branch. Final VCPKG\_ROOT=/Users/core-just/vcpkg VCPKG\_MANIFEST\_INSTALL=OFF python3 script/ai\_check.py passed docs/backlog/plan/diff checks, 235 Python tests, Debug build, and 222/222 CTests; its publisher checks-only phase correctly rejects the uncommitted candidate. The supplied hosted Windows run 34703157457 remains the required confirmation; no hosted success is claimed. |
| Blocker | — |
| Owner | Codex |

## Plan

- Inspect actual nested CMake generator/configuration behavior from the hosted trace.
- Teach the two nested scripts to detect a multi-config nested cache and pass the enclosing configuration to build and CTest, including the multi-config executable path.
- Print the redirected Windows snapshot CTest log before preserving and returning its failure code.
- Add focused static regressions and run Python/CMake/CTest checks with VCPKG_MANIFEST_INSTALL=OFF.

## Children

- None

## Product changes

- None; this is a Windows CI and test-fixture configuration repair.

## Code changes

- tests/CMakeLists.txt; tests/cmake/corecpp_server_consumer_test.cmake.in; tests/cmake/minecraftclone_server_only_test.cmake.in; .github/workflows/snapshot-artifacts.yml; script/tests/test_workflow_contexts.py; generated task/backlog/source-state metadata.
- tests/acceptance/runtime_cli_tests.cmake and script/tests/test_runtime_cli.py now retain and behavior-test exact valid-scenario child diagnostics on failure.

## Resolution

- Changes: Pass the enclosing CTest configuration through both nested fixture commands. Each script retains CMAKE_BUILD_TYPE for nested single-config configuration, detects nested CMAKE_CONFIGURATION_TYPES after configure, then passes that configuration to multi-config build and CTest commands and resolves the consumer executable under its configuration directory. The Windows snapshot workflow now captures CTests status, prints the redirected log, then returns the original CTest failure before any log-print failure. AcceptanceCli.RuntimeModes now preserves the exact valid-scenario child command, result code, stdout, and stderr on failure without changing scenario behavior. The hosted trace has no prior actionable independent diagnostic.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0135
