# MC-AI-0136 — Align presentation pipelines with the active depth attachment

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0136 |
| Level | basic |
| Parent | MC-AI-0035 |
| Status | done |
| Finalized | true |
| Priority | P0 |
| Complexity | low |
| Created | 2026-09-12 |
| Resolved | 2026-09-12 |
| Route | luna |
| Milestone | EarlyDev 0.1.0:4 |
| Kind | bug |
| Baseline commit | — |
| Motivation | Validation reports VUID-vkCmdDraw-dynamicRenderingUnusedAttachments-08914 when the HUD draws inside the common presentation depth attachment because its pipeline declares VK\_FORMAT\_UNDEFINED. |
| Pre-existing context | PresentationRenderer binds a selected D32\_SFLOAT or D16\_UNORM depth image view for every dynamic-rendering frame. Grid and player pipelines already declare that format; the active HUD pipeline omitted it. The fix must preserve the HUD's disabled depth test/write state and must not weaken validation or enable unsupported features. |
| Acceptance | Every active presentation pipeline declares the selected common depth attachment format. The HUD keeps depth testing and writing disabled, focused configuration regression covers grid/player/HUD, and validation-enabled presentation smoke is run when the local environment supports it without changing offscreen/golden output. |
| Evidence | Focused Debug build of mc\_tests passed; DepthFormatTest.\* passed 4/4 and ShaderSpirvTargetTest.\* passed 7/7. Elevated validation-enabled RendererSmokeTest ran from the built candidate with VK\_DRIVER\_FILES=/usr/local/share/vulkan/icd.d/MoltenVK\_icd.json; the HUD-enabled ActiveThirdPersonRendererShowsPlatformPlayersAndTopLeftHud case passed and the log contains no VUID or validation diagnostics. The aggregate smoke exited nonzero because the unrelated CompletedCaptureSurvivesRecreateAndReadsBack byte-retention assertion failed. True offscreen golden could not run in the worker's earlier environment because no Metal-capable Vulkan device was available; unrestricted final artifact acceptance remains required. |
| Blocker | — |
| Owner | Codex |

## Plan

- Declare the selected presentation depth format for grid, player, and HUD pipelines while retaining HUD depth test/write disabled.
- Add a focused configuration regression that covers every active presentation pipeline and run focused renderer checks plus validation-enabled presentation smoke when supported.

## Children

- None

## Product changes

- None

## Code changes

- Added a shared selected-depth-format record for all active presentation pipelines.
- Updated HUD dynamic-rendering pipeline creation to declare the selected depth format with depth test/write disabled.
- Added a focused regression covering every active presentation pipeline.

## Resolution

- Changes: Declared the selected presentation depth format for grid, player, and HUD pipelines while retaining HUD depth test/write disabled; added focused configuration coverage. No offscreen or golden assets changed.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0136
