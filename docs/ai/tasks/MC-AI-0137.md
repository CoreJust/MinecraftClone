# MC-AI-0137 — Repair renderer capture-retention smoke contract

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0137 |
| Level | basic |
| Parent | MC-AI-0035 |
| Status | done |
| Finalized | true |
| Priority | P1 |
| Complexity | low |
| Created | 2026-09-12 |
| Resolved | 2026-09-12 |
| Route | luna |
| Milestone | EarlyDev 0.1.0:4 |
| Kind | bug |
| Baseline commit | — |
| Motivation | The renderer smoke test compares independently rendered HUD captures across time, so dynamic HUD text makes the retention assertion fail even when recreate preserves the completed capture. |
| Pre-existing context | MC-AI-0136 validation smoke showed the existing line-246 rgba8 equality failure while the HUD-enabled active presentation case passed without Vulkan validation diagnostics. The test must prove preservation before another frame is rendered, then validate a later capture by content classes without comparing independent HUD frames. |
| Acceptance | RendererSmokeTest preserves and byte-compares one completed capture across recreate before any subsequent render; a separately rendered later HUD capture is checked for valid dimensions, content classes, and successful capture state without byte equality. No production, golden, or clock changes. |
| Evidence | Focused mc\_renderer\_smoke build passed. Elevated validation-enabled RendererSmokeTest.CompletedCaptureSurvivesRecreateAndReadsBack passed 1/1 with VK\_DRIVER\_FILES=/usr/local/share/vulkan/icd.d/MoltenVK\_icd.json; with HUD disabled, the original capture and a second renderer-owned capture matched in width/height/sRGB/rgba8 across recreate, then a separately rendered resized HUD capture passed dimensions/content classes and HUD draw-count checks. The receipt is /private/tmp/mc-ai-0137-validation-smoke.log and contains no VUID or validation diagnostics. The sandboxed full fast gate passed 223/224 tests but its five-test RendererSmokeTest process timed out after macOS service access failed; the exact unrestricted validation-enabled five-test aggregate then passed 5/5 in 653 ms. Its receipt is /private/tmp/mc-ai-0137-validation-aggregate.log. |
| Blocker | — |
| Owner | Codex |

## Plan

- Move the recreate-retention assertion before any subsequent render.
- Capture a later HUD frame separately and assert validity/content classes without comparing its bytes.

## Children

- None

## Product changes

- None

## Code changes

- Asserted HUD-off completed capture retention through recreate with exact width/height/sRGB/rgba8 equality.
- Validated a subsequent resized dynamic HUD capture without comparing independently rendered bytes.

## Resolution

- Changes: Reworked renderer smoke capture ownership so a HUD-off completed payload remains renderer-owned through recreate and is compared byte-for-byte before any new render; a subsequent resized HUD capture is validated independently by dimensions, content classes, and draw count. No production or golden files changed.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0137
