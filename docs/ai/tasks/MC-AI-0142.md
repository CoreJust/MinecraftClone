# MC-AI-0142 — Drain multiplayer server events within each authoritative tick

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0142 |
| Level | basic |
| Parent | MC-AI-0035 |
| Status | done |
| Finalized | true |
| Priority | P0 |
| Complexity | medium |
| Created | 2026-09-12 |
| Resolved | 2026-09-12 |
| Route | luna |
| Milestone | EarlyDev 0.1.0:4 |
| Kind | bug |
| Baseline commit | — |
| Motivation | The server currently dispatches at most one ENet event per 100 ms tick, so two clients exceed its service rate, starve new handshakes and input, and are eventually disconnected. |
| Pre-existing context | CoreCpp poll\(0\) dispatches at most one event, while each client sends once per 100 ms. |
| Acceptance | The production GameServer loop drains concurrent client events within each bounded 100 ms authoritative tick without allowing more than one move per player per tick. With two real protocol clients sending at the normal cadence, a third client connects, joins, receives authoritative state, and remains serviced; focused coverage uses GameServer::run rather than a manually over-polled helper. |
| Evidence | Configured and built mc\_tests, then passed all 7 focused GameServerTest and ProductionGameServerTest cases; git diff --check passed. The fast source gate is recorded separately. |
| Blocker | — |
| Owner | Codex |

## Plan

- Give the production GameServer loop a bounded tick-duration poll while retaining fixed-step cadence and once-per-player movement.
- Add a real production-loop multiplayer regression with concurrent normal-cadence senders and a third joining client.

## Children

- None

## Product changes

- None

## Code changes

- GameServer drains queued ENet events during each authoritative tick.
- Added production-loop concurrent-client regression coverage.

## Resolution

- Changes: Changed the production server loop to use the bounded authoritative tick duration as its poll timeout, draining queued client events while retaining the once-per-player movement guard; added a real concurrent normal-cadence production-loop regression.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0142
