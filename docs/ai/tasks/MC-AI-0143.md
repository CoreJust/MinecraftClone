# MC-AI-0143 — Keep every authoritative 2x2 player footprint on the platform

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0143 |
| Level | basic |
| Parent | MC-AI-0035 |
| Status | done |
| Finalized | true |
| Priority | P0 |
| Complexity | medium |
| Created | 2026-09-12 |
| Resolved | 2026-09-13 |
| Route | luna |
| Milestone | ED-010-S4 |
| Kind | bug |
| Baseline commit | — |
| Motivation | A player origin in cell 31 places part of its authoritative 2x2 footprint outside the 32x32 platform. |
| Pre-existing context | World movement and collision checks admit origins through cell 31, random spawning can choose that cell, and GameServer scenario-spawn validation follows the old whole-cell boundary. Presentation uses the already-authoritative position and must not compensate with render clamping or epsilon logic. |
| Acceptance | Every authoritative player origin remains within 0 through 30 cells inclusive \(0 through 300000 subcells\) on both axes, so its 2x2 footprint may end exactly at the 32-cell platform edge but never exceed it. Cardinal and diagonal movement accept reaching the final valid origin and reject a further normal-tick move without changing position. Random and explicit server spawns use the same range; explicit cell-30 spawns are valid, cell-31 spawns are rejected, and random spawning selects only valid origins with bounded completion. |
| Evidence | cmake --build --preset debug --target mc\_tests and ctest --preset debug --output-on-failure --no-tests=error -R ^\(WorldTest.\|GameServerLifecycle.\)$: 17/17 passed; python3 script/ai\_check.py --fast: passed. |
| Blocker | — |
| Owner | — |

## Plan

- Define the shared 2x2 footprint origin maximum as cell 30 / subcell 300000 and apply it to World movement and collision admission.
- Generate random origins only within the valid range with a finite candidate selection, and validate explicit GameServer spawn points against the same range.
- Cover each positive axis and diagonal at normal tick speed, the unchanged rejected edge move, explicit cell-30/cell-31 spawns, and bounded random spawning.

## Children

- None

## Product changes

- None

## Code changes

- None

## Resolution

- Changes: Bound World origins, movement, collision admission, random spawning, and scenario spawn validation to the authoritative 2x2 footprint; added deterministic edge and spawn-contract coverage.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0143
