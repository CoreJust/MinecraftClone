# MC-AI-0150 — Stage Windows runtime DLL closure for game and test executables

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0150 |
| Level | basic |
| Parent | MC-AI-0035 |
| Status | done |
| Finalized | true |
| Priority | P0 |
| Complexity | medium |
| Created | 2026-09-13 |
| Resolved | 2026-09-13 |
| Route | luna |
| Milestone | EarlyDev 0.1.0:4 |
| Kind | bug |
| Baseline commit | — |
| Motivation | Windows executables currently fail to start because the build output does not contain the Vulkan loader or the dynamic CoreCpp and vcpkg dependency closure. |
| Pre-existing context | Windows executables exit 0xc0000135. Desktop misses %VULKAN\_SDK%\\\\Bin\\\\vulkan-1.dll; vcpkg applocal does not cover it and Vulkan::Vulkan is an UNKNOWN imported target. Nested/server executables also miss Debug enet.dll, spdlogd.dll and fmtd.dll, while Release uses the non-suffixed names. Keep artifact verification search roots unchanged and do not alter dependency pins. |
| Acceptance | A Windows-only CMake runtime staging helper copies required imported runtime targets and the explicit Vulkan loader beside mc\_main, mc\_tests, optional renderer executables, and cmake/fixtures/corecpp\_server\_consumer. It accepts only executable or shared-library consumers, handles SHARED/MODULE/UNKNOWN imported dependencies without TARGET\_RUNTIME\_DLLS on unsupported target types, and fails at configure time for missing required files or targets. Focused static/configure regressions cover helper use, target roots, supported imported types, and missing dependency/file failures; non-Windows behavior remains unchanged. |
| Evidence | python3 -m unittest script.tests.test\_build\_snapshot -v and script.tests.test\_windows\_runtime\_staging -v passed 24/24. The packaging regression invokes the production packager to create Windows ZIPs, observes vulkan-1.dll absent without --vulkan-runtime, and verifies the supplied loader is present byte-for-byte and in recorded runtime evidence while private-dependencies remains excluded. The configure acceptance case set WIN32 in a synthetic CMake project, used an imported SHARED runtime target, and configured successfully; missing imported target and missing required-file cases failed closed with expected diagnostics. The CoreCpp server-consumer fixture also configured successfully against a temporary local CoreCpp package stub, exercising its real Helpers.cmake include path. git diff --check and python3 -m script.code\_checks passed. A full MinecraftClone configure was not claimed because the available CoreProject2026 prefix was dirty/inexact; no hosted Windows runner was available in this lane. |
| Blocker | — |
| Owner | Luna Windows runtime worker |

## Plan

- Add a Windows-only helper with explicit destination root, required-file validation, and imported target-type gating.
- Apply the helper to desktop game/tests/optional renderer targets and the server-only CoreCpp fixture, with explicit Vulkan loader staging for desktop.
- Add static and configure-time regressions, then run focused tests and CMake validation.

## Children

- None

## Product changes

- None

## Code changes

- cmake/Helpers.cmake and desktop/fixture CMake target wiring for Windows runtime closure.
- script/ci/build_snapshot.py and packaging regression for explicit Windows Vulkan loader inclusion.
- script/tests/test_windows_runtime_staging.py focused static/configure regression.

## Resolution

- Changes: Added a Windows-only post-build staging helper that validates required files, gates imported target types, and copies runtime DLLs into each executable directory. Applied it to mc_main, mc_tests, optional renderer binaries, and the CoreCpp server consumer fixture; explicitly validated and installed the Vulkan SDK loader beside the desktop executable without changing artifact runtime search roots. The Windows package orchestrator accepts --vulkan-runtime, uses its installed directory while resolving the PE dependency closure, and passes the loader to the ZIP packager. Added static, configure-time, and real ZIP-content regressions for target/root wiring, missing dependency/file failures, loader inclusion, provenance, and private-source exclusion.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0150
