# MC-AI-0182 — Use platform-native temporary directories in Windows package regression

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0182 |
| Level | basic |
| Parent | MC-AI-0035 |
| Status | done |
| Finalized | true |
| Priority | P1 |
| Complexity | low |
| Created | 2026-09-13 |
| Resolved | 2026-09-13 |
| Route | luna |
| Milestone | EarlyDev 0.1.0:4 |
| Kind | bug |
| Baseline commit | — |
| Motivation | The hosted Windows test suite fails before exercising packaging because the regression hardcodes macOS-only /private/tmp when creating TemporaryDirectory. |
| Pre-existing context | Exact S4 promotion 668685497505a4253fa7f471750e5a401cff5900. Hosted portability failure is proven in test\_windows\_package\_includes\_explicit\_vulkan\_loader\_in\_zip: TemporaryDirectory\(dir="/private/tmp"\) raises FileNotFoundError on environments without that directory. Scope is the owning packaging regression and task metadata only; preserve the production packager and all runtime search roots. |
| Acceptance | The Windows package regression creates temporary packaging roots through the platform-native temporary-directory mechanism, runs on Linux and Windows without assuming /private/tmp, and preserves explicit Vulkan-loader ZIP inclusion plus private-dependency exclusion. Focused negative and allowed-path checks cover the loader-omitted and loader-supplied packaging branches; no production or Windows-runtime behavior changes. |
| Evidence | The unfixed focused regression was confirmed to depend on dir="/private/tmp"; a platform simulation rejecting that directory failed before packaging with FileNotFoundError. After the fix, script.tests.test\_build\_snapshot.BuildSnapshotTests.test\_windows\_package\_includes\_explicit\_vulkan\_loader\_in\_zip passed 1/1, the complete script.tests.test\_build\_snapshot suite passed 18/18, and python3 script/ai\_check.py --fast passed with full Python tests reused. python3 -m script.code\_checks, ai\_docs.py check, ai\_tasks.py check, ai\_plan.py check, and git diff --check passed. This is local portability evidence only; no hosted Windows runtime or packaging claim. |
| Blocker | — |
| Owner | Codex |

## Plan

- Clarify acceptance and implementation steps.
- Reproduce the failure or confirm the exact platform-specific fixture assumption in the owning test.
- Use tempfile.TemporaryDirectory without a hardcoded directory and retain the ZIP-content assertions.
- Add focused negative and allowed-path checks, then run the test and repository deterministic checks.

## Children

- None

## Product changes

- Windows package regression runs on platform-native temporary-directory environments.

## Code changes

- script/tests/test_build_snapshot.py packaging regression only.
- script/tests/test_build_snapshot.py: platform-native Windows package temporary root and negative archive-path assertion.

## Resolution

- Changes: Replaced the hardcoded /private/tmp packaging fixture root with a platform-native TemporaryDirectory and resolved the temporary root before invoking the production packager, preserving loader-omitted and loader-supplied ZIP assertions and adding explicit private-dependency exclusion to the negative branch. No production or Windows-runtime behavior changed.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0182
