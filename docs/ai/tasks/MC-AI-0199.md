# MC-AI-0199 — Move Windows runtime staging to the top-level CMake directory

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0199 |
| Level | basic |
| Parent | MC-AI-0036 |
| Status | done |
| Finalized | true |
| Priority | P0 |
| Complexity | low |
| Created | 2026-09-13 |
| Resolved | 2026-09-13 |
| Route | luna |
| Milestone | ED-010-S5 |
| Kind | bug |
| Baseline commit | — |
| Motivation | Windows hosted configure rejects the mc\_main runtime staging command because add\_custom\_command\(TARGET\) is declared from src/client while mc\_main is created in the top-level CMakeLists. |
| Pre-existing context | src/client/CMakeLists invokes mc\_stage\_windows\_runtime\(mc\_main ...\) after shader setup, but CMake requires TARGET post-build commands to be declared in the directory that created the target. The top-level CMakeLists creates mc\_main before add\_subdirectory\(src\); shader copying and install rules currently live in src/client. |
| Acceptance | Windows desktop configure and build can register runtime staging for mc\_main from the target-owning top-level directory; the existing shader copy and mc\_main/Vulkan-loader/shader install behavior remains intact, and macOS/Android configuration behavior is unchanged. |
| Evidence | python3 -m unittest script.tests.test\_windows\_runtime\_staging -v passed 6/6. cmake --preset debug configured successfully and cmake --build --preset debug --target mc\_main -j 4 completed with shader assets copied. python3 script/ai\_docs.py refresh and check passed. git diff --check passed. No Windows hosted rerun or windowed runtime was performed in this lane. |
| Blocker | — |
| Owner | Codex |

## Plan

- Move only mc_stage_windows_runtime(mc_main ...) and the associated desktop mc_main/Vulkan-loader/shader install rules to the top-level CMakeLists after add_subdirectory(src), preserving shader generation/copy registration in src/client.
- Run the focused Windows runtime staging regression, fast task gate, and bounded Luna/high staged review; commit exactly once with Task-ID MC-AI-0199.

## Children

- None

## Product changes

- None

## Code changes

- CMakeLists.txt and src/client/CMakeLists.txt; relocate target-owned Windows runtime staging and desktop install rules.
- CMakeLists.txt and src/client/CMakeLists.txt relocate target-owned staging/install rules; script/tests/test_windows_runtime_staging.py asserts the owning-directory contract; generated task/backlog/code-state metadata refreshed.

## Resolution

- Changes: Moved mc_main Windows runtime staging and desktop install rules to the top-level CMakeLists after add_subdirectory(src), while retaining shader generation and executable shader copying in src/client.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0199
