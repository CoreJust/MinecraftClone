# MC-AI-0200 — MC-AI-0200

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0200 |
| Level | basic |
| Parent | MC-AI-0035 |
| Status | done |
| Finalized | true |
| Priority | P0 |
| Complexity | low |
| Created | 2026-09-13 |
| Resolved | 2026-09-13 |
| Route | luna |
| Milestone | EarlyDev 0.1.0:4 |
| Kind | bug |
| Baseline commit | — |
| Motivation | The hosted Windows runtime-staging regression fails while configuring the CoreCpp server-consumer fixture because its local package stub omits the fmt::fmt runtime dependency required by the production helper. |
| Pre-existing context | Maintenance repair on exact base b679d0002adccdf9350346da142040d3a53fecce after the hosted Windows fixture selected MinGW and failed Helpers.cmake:105 with Windows runtime staging dependency is missing: fmt::fmt. Production missing-dependency fail-fast behavior must remain unchanged. |
| Acceptance | The CoreCpp server-consumer Windows fixture defines realistic MinGW-style UNKNOWN imported runtime targets and existing files for fmt::fmt, spdlog::spdlog, and unofficial::enet::enet, so the production staging helper configures successfully. The helper continues to fail fast when a required dependency target is absent. A focused regression requires the fmt::fmt imported target and staged DLL path in the local package stub and configures the fixture. |
| Evidence | python3 -m unittest script.tests.test\_windows\_runtime\_staging -v passed 6/6. The CoreCpp server-consumer fixture regression now creates fmt.dll, spdlog.dll, and enet.dll files and defines fmt::fmt, spdlog::spdlog, and unofficial::enet::enet as UNKNOWN IMPORTED targets with concrete IMPORTED\_LOCATION paths; the fixture configures successfully. The same suite's missing-dependency and missing-required-file cases still fail closed with the expected diagnostics. python3 script/ai\_docs.py refresh and check passed; no hosted Windows runner or production Windows build was run in this lane. |
| Blocker | — |
| Owner | Codex |

## Plan

- Extend the local CoreCpp package stub with MinGW-style UNKNOWN imported runtime targets and fixture DLL files for fmt, spdlog, and enet.
- Require the fmt target and DLL path in the focused configure regression, then retain missing-dependency fail-fast coverage.

## Children

- None

## Product changes

- Windows server-consumer fixture coverage now models the runtime dependency closure required by the staging helper.

## Code changes

- script/tests/test_windows_runtime_staging.py: add realistic imported runtime targets/files to the CoreCpp package stub and assert the fmt::fmt fixture contract.

## Resolution

- Changes: Updated only the CoreCpp server-consumer test package stub to model the MinGW runtime dependency closure, including the missing fmt::fmt target and staged DLL path; tightened the focused regression to require that contract. Production Helpers.cmake behavior was unchanged.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0200
