# MC-AI-0200 — Fix MSVC bot input distributions

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0200 |
| Level | basic |
| Parent | MC-AI-0036 |
| Status | done |
| Finalized | true |
| Priority | P0 |
| Complexity | low |
| Created | 2026-09-13 |
| Resolved | 2026-09-13 |
| Route | luna |
| Milestone | ED-010-S5 |
| Kind | bug |
| Baseline commit | — |
| Motivation | Hosted Windows run34765220976 fails BotClient.cpp line 9 because MSVC rejects std::uniform\_int\_distribution&lt;uint8\_t&gt; with C2338. |
| Pre-existing context | The bot currently uses uint8\_t and int8\_t as uniform\_int\_distribution template arguments. MSVC STL supports the distribution only for its listed integer types, so both byte aliases are rejected even though the host compiler accepts them. The existing gameplay contract is a persistent random direction changed with probability 1/50 per input call, with each selected component at -127 or 127. |
| Acceptance | BotClient input compiles with MSVC by using supported integer distribution types for the existing 1/50 change and -1..1 direction ranges, while converting generated direction values to uint8\_t and preserving bot behavior. |
| Evidence | Focused debug build compiled BotClient.cpp and linked mc\_main and mc\_tests successfully. ctest --preset debug -R GameServer\|Client\|Server passed 42/42. No new test was added because existing BotClient input is private and no behavior contract requires test coverage for this portability-only template change. |
| Blocker | — |
| Owner | Codex |

## Plan

- Clarify acceptance and implementation steps.
- Use supported int distributions for change selection and signed direction generation while preserving existing ranges and explicit output conversion.

## Children

- None

## Product changes

- None; this is a portability correction preserving existing bot input behavior.

## Code changes

- src/client/BotClient.cpp uses int distributions for MSVC portability and keeps uint8_t conversion for generated bot input.

## Resolution

- Changes: Changed both BotClient random distributions to int, retaining the explicit uint8_t casts for generated direction bytes and the existing ranges and update probability.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0200
