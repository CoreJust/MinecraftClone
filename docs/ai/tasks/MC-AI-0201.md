# MC-AI-0201 — Stabilize normal-cadence join fixture observation

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0201 |
| Level | basic |
| Parent | MC-AI-0035 |
| Status | done |
| Finalized | true |
| Priority | P1 |
| Complexity | low |
| Created | 2026-09-13 |
| Resolved | 2026-09-13 |
| Route | luna |
| Milestone | EarlyDev 0.1.0:4 |
| Kind | bug |
| Baseline commit | — |
| Motivation | Hosted Mac evidence showed the production regression could skip the newcomer join entirely when its local sender-count readiness predicate was false, masking the actual starvation behavior. |
| Pre-existing context | Exact maintenance base b679d0002adccdf9350346da142040d3a53fecce. Hosted failure reported normal\_cadence\_backlog\_created=false and joined=false. Local sender counts establish only that sender threads reached a preparation threshold; they are not evidence of a server backlog. Scope is tests/core/game\_server\_tests.cpp and task metadata only; preserve production code, timeouts, and authoritative assertions. |
| Acceptance | The production normal-cadence multiplayer regression proves that two established senders reach the intended preparation threshold, independently attempts and observes a newcomer join, and retains authoritative-state plus bounded join-elapsed assertions. It does not label local sender counts as server backlog evidence, change production behavior, or increase timeouts. |
| Evidence | Built mc\_tests against the exact099 release CoreProject2026/CoreCpp packages and passed ProductionGameServerTest.ConcurrentNormalCadenceInputsDoNotStarveNewJoin 20 consecutive times \(1/1 repeated 20, 100% pass, 27.83 s\). The focused candidate renames the local sender-count predicate to normal\_cadence\_inputs\_prepared and removes the short-circuit before join\(\); git diff --check, python3 script/ai\_tasks.py check, and python3 script/ai\_check.py --fast passed. |
| Blocker | — |
| Owner | Codex |

## Plan

- Inspect the hosted-failure fixture precondition and distinguish local sender preparation from server backlog evidence.
- Always attempt the newcomer join independently of the sender preparation observation.
- Build mc_tests, run the exact cadence regression repeatedly, and run the fast deterministic gate.

## Children

- None

## Product changes

- The normal-cadence join regression observes newcomer service independently from sender preparation.

## Code changes

- tests/core/game_server_tests.cpp: truthfully rename sender preparation and remove the join short-circuit.

## Resolution

- Changes: Renamed the sender-count readiness observation to normal_cadence_inputs_prepared and always attempted the newcomer join independently, while retaining the established-sender threshold assertion and authoritative-state and join-elapsed assertions. No production or timeout changes.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0201
