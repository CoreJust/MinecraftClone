# MC-AI-0202 — Fix MSVC from_chars string_view portability

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0202 |
| Level | basic |
| Parent | MC-AI-0036 |
| Status | done |
| Finalized | true |
| Priority | P0 |
| Complexity | low |
| Created | 2026-09-13 |
| Resolved | 2026-09-13 |
| Route | luna |
| Milestone | ED-010-S5 |
| Kind | bug |
| Baseline commit | — |
| Motivation | Hosted Windows run34766768832 fails src/main.cpp parseAddress because MSVC std::from\_chars cannot accept std::string\_view iterators. |
| Pre-existing context | parseAddress currently passes std::string\_view iterators to std::from\_chars and compares the returned pointer with string\_view::end\(\). The MSVC standard library requires a supported pointer range for this overload, producing C2665 and then C2679 in the hosted Windows build. The requested correction preserves the existing uint32\_t port parse and validation. |
| Acceptance | The Windows MSVC build accepts desktop address parsing by passing a contiguous pointer range to std::from\_chars and comparing its returned end pointer with the same range endpoint; existing IP:PORT parsing behavior remains unchanged. |
| Evidence | Hosted Windows run34766768832 reports MSVC C2665/C2679 for std::from\_chars called with std::string\_view iterators in src/main.cpp. Changed parseAddress to pass port\_string.data\(\) through port\_string.data\(\) + port\_string.size\(\) and compare the returned end pointer with that same endpoint. cmake --build --preset debug --target mc\_main -j 4 passed; tests/acceptance/client\_launch\_test.py build/debug/mc\_main -v passed 2/2, including invalid port cases; python3 script/ai\_docs.py check passed; python3 script/ai\_check.py --fast exited 0 with docs, backlog, plan, diff, and common checks passing, while the expected dirty-tree source-check exception was recorded. |
| Blocker | — |
| Owner | Codex |

## Plan

- Change only parseAddress's std::from_chars call to use port_string.data() through port_string.data() + port_string.size(), and compare the returned end pointer with that same endpoint.
- Run focused compile or CLI parsing checks, the fast task gate, and one bounded Luna/high review of the staged candidate; commit exactly once with Task-ID MC-AI-0202.

## Children

- None

## Product changes

- None; this is a Windows compiler portability correction that preserves address parsing behavior.

## Code changes

- src/main.cpp uses contiguous data pointers for the desktop std::from_chars port range and returned-end comparison; docs/code/source_state.json was refreshed.

## Resolution

- Changes: Updated only desktop parseAddress's std::from_chars range and end-pointer comparison to use contiguous string_view data pointers, preserving the existing port conversion and validation behavior.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0202
