# MC-AI-0202 — Fix MSVC from_chars string_view portability

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0202 |
| Level | basic |
| Parent | MC-AI-0036 |
| Status | active |
| Finalized | false |
| Priority | P0 |
| Complexity | low |
| Created | 2026-09-13 |
| Resolved | — |
| Route | luna |
| Milestone | ED-010-S5 |
| Kind | bug |
| Baseline commit | — |
| Motivation | Hosted Windows run34766768832 fails src/main.cpp parseAddress because MSVC std::from\_chars cannot accept std::string\_view iterators. |
| Pre-existing context | parseAddress currently passes std::string\_view iterators to std::from\_chars and compares the returned pointer with string\_view::end\(\). The MSVC standard library requires a supported pointer range for this overload, producing C2665 and then C2679 in the hosted Windows build. The requested correction preserves the existing uint32\_t port parse and validation. |
| Acceptance | The Windows MSVC build accepts desktop address parsing by passing a contiguous pointer range to std::from\_chars and comparing its returned end pointer with the same range endpoint; existing IP:PORT parsing behavior remains unchanged. |
| Evidence | — |
| Blocker | — |
| Owner | Codex |

## Plan

- Change only parseAddress's std::from_chars call to use port_string.data() through port_string.data() + port_string.size(), and compare the returned end pointer with that same endpoint.
- Run focused compile or CLI parsing checks, the fast task gate, and one bounded Luna/high review of the staged candidate; commit exactly once with Task-ID MC-AI-0202.

## Children

- None

## Product changes

- None

## Code changes

- None

## Resolution

- Changes: —

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0202
