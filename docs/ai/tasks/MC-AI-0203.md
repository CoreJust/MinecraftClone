# MC-AI-0203 — Stage the pinned Windows Vulkan runtime

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0203 |
| Level | basic |
| Parent | MC-AI-0035 |
| Status | done |
| Finalized | true |
| Priority | P0 |
| Complexity | low |
| Created | 2026-09-13 |
| Resolved | 2026-09-13 |
| Route | luna |
| Milestone | EarlyDev 0.1.0:4 |
| Kind | bug |
| Baseline commit | b866217b9209096c81e71b0baffde35012712527 |
| Motivation | Hosted Windows configuration failed because the pinned SDK lacked VULKAN\_SDK/Bin/vulkan-1.dll while CMake and packaging require a real Vulkan loader. |
| Pre-existing context | Hosted failure run34756197005 on the exact S4 candidate configured the pinned SDK successfully but stopped before compilation when the expected Windows Vulkan runtime was absent. Acquire the exact official LunarG runtime archive independently and fail closed on any identity or content mismatch. |
| Acceptance | The hosted Windows release workflow downloads the pinned LunarG 1.4.357.0 runtime-components ZIP, verifies its SHA-256 before extraction, requires a real non-empty x64/vulkan-1.dll member, copies it into the acquired SDK's Bin directory consumed by CMake, and retains package-desktop's explicit --vulkan-runtime staging. Non-Windows workflow behavior remains unchanged; focused workflow tests cover ordering, identity, and fail-fast checks. |
| Evidence | Workflow regression test covers the exact runtime-components URL and SHA-256, verification before extraction, the non-empty x64 loader member, copying into VULKAN\_SDK/Bin before CMake, and preserved package handoff. Hosted Windows execution remains pending. |
| Blocker | — |
| Owner | Codex |

## Plan

- Download the pinned official LunarG runtime archive and verify its SHA-256 before extraction.
- Validate and copy its x64 loader into the acquired SDK Bin directory, then add a focused workflow regression test.

## Children

- None

## Product changes

- Hosted Windows release configuration now stages the pinned official Vulkan loader required by desktop runtime packaging.

## Code changes

- .github/workflows/snapshot-artifacts.yml: hash-check the pinned runtime archive, require its non-empty x64 loader, copy it into the acquired SDK, and preserve explicit package staging.
- script/tests/test_workflow_contexts.py: cover archive identity, loader path, ordering, and explicit package staging.

## Resolution

- Changes: The hosted Windows release step now hash-verifies the pinned official LunarG runtime archive, stages its validated x64 loader into the acquired SDK Bin directory before configure, and retains package-desktop's explicit build\install\vulkan-1.dll handoff.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0203
