# MC-AI-0203 — Stage Windows runtime DLLs for nested server-only tests

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0203 |
| Level | basic |
| Parent | MC-AI-0036 |
| Status | done |
| Finalized | true |
| Priority | P0 |
| Complexity | low |
| Created | 2026-09-13 |
| Resolved | 2026-09-13 |
| Route | luna |
| Milestone | ED-010-S5 |
| Kind | bug |
| Baseline commit | b777950b619e360d9b575cec5d5d767271296e1a |
| Motivation | Hosted Windows artifact run 34767535367 built mc\_tests.exe but GoogleTest discovery failed with exit code 0xc0000135 because runtime DLLs were absent from the nested test output directory. |
| Pre-existing context | The main desktop target already stages Windows runtime dependencies, while tests/CMakeLists.txt only stages mc\_tests when MC\_BUILD\_CLIENT is enabled. The nested server-only build disables MC\_BUILD\_CLIENT but links the CoreCpp network/runtime closure and vcpkg DLL-backed libraries. |
| Acceptance | The nested MinecraftClone.ServerOnlyBuild CTest launches mc\_tests.exe on Windows by staging every dynamic dependency required by the server-only target; the server-only configuration still excludes optional graphics targets. |
| Evidence | Hosted Windows run 34767535367 built the release but failed MinecraftClone.ServerOnlyBuild at GoogleTest discovery with exit code 0xc0000135. Updated tests/CMakeLists.txt to stage CoreCpp::Core, CoreCpp::Runtime, CoreCpp::RuntimeNetwork, CoreProject2026::CoreLang, fmt, spdlog, enet, and libmpfr/libgmp numeric DLLs; focused runtime staging tests, full Python tests, docs check, and ai\_check --fast pass. Hosted rerun is required for final release evidence. |
| Blocker | — |
| Owner | Codex |

## Plan

- Stage the server-only runtime dependency closure for mc_tests on Windows without loading optional graphics targets; update the source contract and run focused checks plus the hosted artifact workflow.

## Children

- None

## Product changes

- Windows server-only validation runs successfully during release packaging.

## Code changes

- tests/CMakeLists.txt stages CoreCpp, CoreProject2026, fmt, spdlog, and enet runtime DLLs for mc_tests on Windows.

## Resolution

- Changes: Stage the Windows server-only mc_tests runtime closure, including vcpkg CoreLang numeric DLLs, and fail configure when numeric DLL discovery is empty.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0203
