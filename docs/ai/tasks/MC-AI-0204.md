# MC-AI-0204 — Stage GoogleTest runtime for nested server-only tests

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0204 |
| Level | basic |
| Parent | MC-AI-0036 |
| Status | done |
| Finalized | true |
| Priority | P0 |
| Complexity | low |
| Created | 2026-09-13 |
| Resolved | 2026-09-13 |
| Route | luna |
| Milestone | ED-010-S5 |
| Kind | bug |
| Baseline commit | — |
| Motivation | Hosted Windows run 34769155176 copied numeric, fmt, and spdlog DLLs but mc\_tests.exe still exited 0xc0000135; the server-only staging list omitted GTest::gtest even though the client staging list includes it. |
| Pre-existing context | tests/CMakeLists.txt has separate server-only and client Windows runtime staging calls. The server-only call must include the same test executable runtime dependency for dynamic vcpkg configurations. |
| Acceptance | The nested MinecraftClone.ServerOnlyBuild CTest stages the GoogleTest runtime DLL when the Windows package uses a dynamic GoogleTest build, allowing mc\_tests.exe discovery to start. |
| Evidence | Hosted Windows run 34769155176 copied the numeric, fmt, and spdlog runtime DLLs but MinecraftClone.ServerOnlyBuild still failed mc\_tests discovery with exit code 0xc0000135. Added GTest::gtest to the server-only staging call; focused runtime staging tests pass and the hosted rerun is required. |
| Blocker | — |
| Owner | Codex |

## Plan

- Add GTest::gtest to the server-only mc_tests runtime closure, update the static contract, run focused checks, and verify the hosted Windows artifact job.

## Children

- None

## Product changes

- Windows server-only validation starts successfully with dynamic GoogleTest.

## Code changes

- tests/CMakeLists.txt includes GTest::gtest in the server-only mc_tests runtime staging dependencies.

## Resolution

- Changes: Added GTest::gtest to the nested server-only mc_tests Windows runtime staging closure.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0204
