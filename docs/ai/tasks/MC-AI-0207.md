# MC-AI-0207 — Replace S6 preview terrain with mature large-world chunk streaming and rendering architecture

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0207 |
| Level | basic |
| Parent | MC-AI-0037 |
| Status | done |
| Finalized | true |
| Priority | P0 |
| Complexity | high |
| Created | 2026-09-18 |
| Resolved | 2026-09-19 |
| Route | luna |
| Milestone | ED-010-S6 |
| Kind | feature |
| Baseline commit | — |
| Motivation | Replace S6 preview terrain with mature large-world chunk streaming and rendering architecture |
| Pre-existing context | Owner rejected the fixed spawn preview, isolated surface chunks, 29x29 coverage, and performance collapse. S6 must establish mature foundations before future blocks, mechanics, entities, and graphics increase workload. |
| Acceptance | One shared sparse-world lifecycle supports local and network play; residency follows the player as a camera-independent circle with a 45-chunk radius. Camera rotation never removes or hides resident chunks. Generation fills fixed radius-3 and radius-8 circles first, then a movement/view-oriented medium-priority ellipse reaching the outer circle, then rate-limited background chunks until a stationary player receives the complete circle. Chunks enter ahead and evict behind without near-camera holes; generation is asynchronous, deterministic, and bounded; CoreLang configures the native height-map kernel while C++ owns blocks; neighbor-aware meshes have no floating slabs or hidden internal faces; renderer incrementally uploads and culls chunks; CPU, GPU, network, and queue memory are bounded. Stone retains texture over the visible range, with distant fog flowing into a gradient sky. Movement remains continuous at the streaming edge; HUD shows acceleration speed on its own line; wave spacing is 128 blocks. Target stable 120 presented FPS in real Mac streaming, with recorded p50/p95/p99 frame and generation timings. A reusable text API lays out arbitrary-length colored labels for both depth-tested world placement and screen-space GUI; HUD is a GUI client, and GUI records in its own rendering stage rather than relying on HUD-specific shader layout. |
| Evidence | 2026-09-19 local and owner acceptance: 335 top-level CTest cases cover circular residency, priority bands, asynchronous streaming, movement, wrapped boundaries, renderer correctness, text/GUI, and CoreLang generation. The fast-flight frontier integration test passed five consecutive controlled-credit runs. The exact normal macOS candidate passed owner playtest, including the final 65535-to-0 seam crossing. Foreground 1920x1080 HUD-on maximum-capacity benchmark sustained nominal 120 Hz with 8.84 ms p99; receipt /private/tmp/mc-s6-directional-final.json. ai\_check.py --fast passed. |
| Blocker | — |
| Owner | Codex |

## Plan

- Clarify acceptance and implementation steps.
- Replace preview DTOs with compact height-tile/chunk lifecycle messages and removals; implement moving interest diff and directional priority; build neighbor-aware heightfield/voxel surface meshes; use persistent chunk GPU allocations with incremental upload and culling; add deterministic lifecycle, seam, cancellation, budget, transport, and benchmark coverage; validate the exact normal Mac candidate.
- Replace the HUD-shaped text shader and fixed word packing with dynamic glyph batches; add a depth-tested world-text path and a distinct screen-space GUI pass, then prove long labels, color spans, and placement with focused tests.

## Children

- None

## Product changes

- S6 establishes mature large-world generation, streaming, and rendering foundations with a view/movement-oriented terrain ellipse and stable 120 FPS target.
- S6 establishes mature large-world generation, streaming, and rendering foundations with camera-independent circular residency, tiered directional generation priority, and a stable 120 FPS target.

## Code changes

- Shared chunk lifecycle, height-map transport, moving server interest, bounded scheduling/cache eviction, neighbor-aware meshing, persistent renderer allocations, culling, instrumentation, and performance gates.
- Reusable glyph layout and GPU text path; separate world-text and GUI recording stages with HUD as a GUI client.

## Resolution

- Changes: Replaced fixed preview terrain with a bounded camera-independent 45-chunk circular residency lifecycle, tiered directional generation priorities, background fill, asynchronous workers, revisioned height-tile transport, neighbor-aware detailed meshes, incremental Vulkan uploads and culling, textured stone, fog/sky integration, reusable GUI/world text, semantic HUD, selectable acceleration through 500x, continuous movement, and shortest-path presentation across the wrapped world seam. Added deterministic unit, integration, transport, sustained-flight, GPU, benchmark, and seam regressions.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0207
