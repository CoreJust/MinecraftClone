# MC-AI-0208 — Repair Windows server-only DLL discovery

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0208 |
| Level | basic |
| Parent | MC-AI-0035 |
| Status | done |
| Finalized | true |
| Priority | P0 |
| Complexity | low |
| Created | 2026-09-13 |
| Resolved | 2026-09-13 |
| Route | luna |
| Milestone | EarlyDev 0.1.0:4 |
| Kind | bug |
| Baseline commit | — |
| Motivation | Hosted Windows run 34759763918 reaches the nested server-only build, creates tests/Release/mc\_tests.exe, then GoogleTest discovery fails with 0xc0000135 because the discovery process cannot resolve its runtime DLL closure. |
| Pre-existing context | Maintenance repair after MC-AI-0150/MC-AI-0206 on the exact current snapshot branch. The nested fixture builds mc\_tests under tests/Release, but runtime staging is currently conditional on MC\_BUILD\_CLIENT and therefore does not run for the server-only build; gtest\_discover\_tests then launches an executable whose DLL dependencies are not beside it. Scope is the server-only test/CMake wiring and focused task evidence only; do not alter gameplay, dependency pins, artifact search roots, or desktop behavior. |
| Acceptance | The nested MinecraftClone.ServerOnlyBuild test discovers and runs mc\_tests on Windows Debug and Release without 0xc0000135, with required runtime DLLs resolved from the executable directory; non-Windows behavior and gameplay remain unchanged. |
| Evidence | Hosted Windows run 34759763918 was diagnosed from the supplied trace: nested tests/Release/mc\_tests.exe was built, then GoogleTest discovery failed with 0xc0000135. The focused Windows runtime-staging suite passed 6/6; clean macOS MC\_BUILD\_CLIENT=OFF mc\_tests build and CTest passed 142/142 including MinecraftClone.ServerOnlyBuild; a configure-only WIN32=TRUE server-only simulation generated successfully. No hosted Windows rerun is available in this lane, so Windows runtime execution remains unclaimed. |
| Blocker | — |
| Owner | Codex Windows server-only worker |

## Plan

- Inspect the nested server-only configure/build/discovery flow and identify the owning target-directory runtime staging gap.
- Apply the smallest portable CMake/test fix so the server-only mc_tests executable has its Windows runtime DLL closure before GoogleTest discovery.
- Run focused structural/configure checks and record the hosted failure evidence plus local platform limitations.

## Children

- None

## Product changes

- Windows server-only test discovery now receives its runtime staging before GoogleTest enumeration.

## Code changes

- tests/CMakeLists.txt and/or the owning server-only CMake fixture wiring; task ledger and generated task receipt.
- tests/CMakeLists.txt: stage mc_tests on Windows for both client and server-only configurations, using the server-only dependency closure when MC_BUILD_CLIENT=OFF.

## Resolution

- Changes: Moved mc_tests Windows runtime staging outside the client-only block and selected the graphics-free server dependency closure before GoogleTest discovery.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0208
