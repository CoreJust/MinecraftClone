# MC-AI-0211 — Complete S6 late artifact delivery

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0211 |
| Level | basic |
| Parent | MC-AI-0037 |
| Status | done |
| Finalized | true |
| Priority | P0 |
| Complexity | high |
| Created | 2026-09-19 |
| Resolved | 2026-09-19 |
| Route | luna |
| Milestone | EarlyDev 0.1.0:6 |
| Kind | bug |
| Baseline commit | — |
| Motivation | Complete the published S6 prerelease after the first hosted artifact run exposed cross-platform compiler and loaded-runner test orchestration failures. |
| Pre-existing context | The canonical S6 tag is immutable. Workflow fixes may be dispatched from ai-dev while all artifact jobs check out and package the exact tagged source. Do not move the tag or change gameplay bytes. |
| Acceptance | The late-artifact workflow builds the immutable S6 source and dependency locks with portable hosted compiler settings, isolates the long-running macOS sustained-flight and nested server-only checks from suite contention without weakening their assertions, and preserves exact source provenance. |
| Evidence | Static workflow contracts verify Windows clang-cl selection, Android Clang compatibility flags supplied outside the immutable source tree, isolated macOS long-running checks, unchanged dependency locks, and fail-closed command handling. Focused workflow/build-snapshot tests passed 35/35; the complete Python suite and ai\_check.py --fast passed. |
| Blocker | — |
| Owner | — |

## Plan

- None

## Children

- None

## Product changes

- No gameplay behavior changes; complete the supported-platform S6 delivery.

## Code changes

- Make the late artifact workflow compile the pinned CoreLang package portably and isolate long-running macOS acceptance tests from suite contention.

## Resolution

- Changes: Updated only late-artifact orchestration and its deterministic tests: Windows private packages use hosted clang-cl, Android suppresses the pinned CoreLang designated-initializer ordering diagnostic through a temporary CMake include, and macOS executes sustained-flight and nested server-only validation separately from the loaded aggregate. The immutable S6 tag and dependency locks remain unchanged.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0211
