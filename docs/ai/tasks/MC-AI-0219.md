# MC-AI-0219 — Use one Windows toolchain and bounded server-only test plan

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0219 |
| Level | basic |
| Parent | MC-AI-0037 |
| Status | done |
| Finalized | true |
| Priority | P0 |
| Complexity | medium |
| Created | 2026-09-19 |
| Resolved | 2026-09-19 |
| Route | luna |
| Milestone | EarlyDev 0.1.0:6 |
| Kind | bug |
| Baseline commit | — |
| Motivation | Run 35463551648 built successfully but mixed clang-cl static dependencies with an MSVC game, producing 0xC0000409 failures across networking, CoreLang, scenarios, and acceptance; the nested server-only wrapper separately hit its 180-second outer timeout. |
| Pre-existing context | Keep immutable source and dependencies. Use one ABI-compatible Windows compiler for all C++ and reuse the proven macOS nested server-only separation without excluding ordinary Windows tests. |
| Acceptance | Windows private dependencies and the game use the same clang-cl compiler and warning launcher; hosted CTest runs the ordinary suite once, excludes only the nested server-only wrapper from that pass, patches the generated nested plan to avoid duplicate GameServerPreview tests, and then executes the remaining server-only plan without an outer timeout. Workflow tests lock compiler and test-plan boundaries. |
| Evidence | Run 35463551648 compiled and linked the complete game, then showed systemic 0xC0000409 failures only in the mixed MSVC/clang-cl runtime plus a distinct 180-second nested server-only timeout. Workflow regressions cover compiler consistency and bounded nested execution; final hosted acceptance remains on MC-AI-0037. |
| Blocker | — |
| Owner | — |

## Plan

- None

## Children

- None

## Product changes

- No gameplay change.

## Code changes

- Add clang-cl and launcher arguments to the Windows game configure; generate a bounded server-only test-plan patcher; run outer and nested Windows suites separately.

## Resolution

- Changes: Configured the Windows game with the same clang-cl launcher as its private dependencies and split the generated server-only wrapper from the ordinary CTest pass while removing duplicate preview cases from the nested suite.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0219
