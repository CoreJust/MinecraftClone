# MC-AI-0221 — Use native MSVC for the complete Windows artifact

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0221 |
| Level | basic |
| Parent | MC-AI-0037 |
| Status | done |
| Finalized | true |
| Priority | P0 |
| Complexity | medium |
| Created | 2026-09-19 |
| Resolved | 2026-09-19 |
| Route | luna |
| Milestone | EarlyDev 0.1.0:6 |
| Kind | bug |
| Baseline commit | — |
| Motivation | Run 35465157543 proved unified clang-cl still aborts CoreLang 0.1.2 at runtime with 0xc0000409, cascading through terrain, server, scenario, and CLI tests, while the pinned CoreProject2026 source already owns an explicit supported MSVC warning/runtime path. |
| Pre-existing context | Keep immutable S6 source/tag and dependency pins. Change only late-artifact orchestration. Prefer the upstream native Windows compiler contract over another clang compatibility exception. |
| Acceptance | Windows private dependencies and MinecraftClone select one native MSVC toolchain through the initialized Visual Studio environment, preserve each project native MSVC warning policy, and keep the bounded server-only test plan. Deterministic workflow tests lock compiler selection, fail-closed command handling, and unchanged dependency pins; hosted artifact acceptance remains solely on MC-AI-0037. |
| Evidence | Run 35465157543 compiled unified clang-cl successfully but 36 CoreLang-dependent runtime tests aborted with 0xc0000409 while unrelated tests passed, isolating the compiler/runtime path. The replacement native-MSVC workflow contract and fail-closed command handling pass 37/37 focused workflow/package tests, backlog validation, and git diff checks. Final hosted acceptance remains aggregate-owned. |
| Blocker | — |
| Owner | — |

## Plan

- Replace the clang-cl launcher path with native MSVC for private dependencies and the game; retain bounded server-only orchestration; lock the exact workflow contract; run focused policy tests and hosted acceptance.

## Children

- None

## Product changes

- No gameplay change.

## Code changes

- Use the Visual Studio environment default cl compiler consistently across the Windows dependency and game configurations and remove the clang-only launcher.

## Resolution

- Changes: Removed the clang-cl compiler and launcher overrides from the immutable-source Windows artifact path. Both private dependencies and MinecraftClone now use the Visual Studio environment native MSVC compiler and each project native warning policy; the bounded nested server-only plan remains unchanged.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0221
