# MC-AI-0225 — Use the enclosing generator for nested validation

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0225 |
| Level | basic |
| Parent | MC-AI-0037 |
| Status | done |
| Finalized | true |
| Priority | P0 |
| Complexity | low |
| Created | 2026-09-20 |
| Resolved | 2026-09-20 |
| Route | luna |
| Milestone | EarlyDev 0.1.0:6 |
| Kind | bug |
| Baseline commit | ce399b0584b5eb5adfc818b6258561ea1747f8da |
| Motivation | The formal aggregate gate exposed an intermittent fresh Unix Makefiles nested build failure with disappearing object dependency paths; the enclosing project already standardizes on Ninja. |
| Pre-existing context | MC-AI-0224 removed workflow patching and duplicate preview execution. This follow-up makes the nested build generator deterministic instead of relying on the host default. |
| Acceptance | The nested MinecraftClone server-only package build uses the same explicit CMake generator as its enclosing release build, succeeds from a fresh build directory, and retains the source-owned preview exclusion from MC-AI-0224 on macOS and Windows. |
| Evidence | The nested fixture now passes -G @CMAKE\_GENERATOR@. All 19 workflow-context tests passed, then a fresh Debug nested Ninja configure/build and the complete non-preview server-only CTest succeeded against exact CoreCpp 1a1d4095bd553ed1ea382523eecd22c9748bbe9f and CoreProject2026 048a3abb8f3d18d97c41dbd618eb37fd0247f285 packages. |
| Blocker | — |
| Owner | — |

## Plan

- Propagate the enclosing CMake generator into the nested configure command, cover the contract statically, and rerun the fresh nested package build.

## Children

- None

## Product changes

- No gameplay behavior changes.

## Code changes

- Pass the enclosing CMake generator explicitly to the nested server-only configure.

## Resolution

- Changes: Propagated the enclosing CMake generator into the nested server-only configure and added a static contract check.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0225
