# MC-AI-0228 — Cache translation-invariant terrain interest order

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0228 |
| Level | basic |
| Parent | MC-AI-0037 |
| Status | done |
| Finalized | true |
| Priority | P0 |
| Complexity | medium |
| Created | 2026-09-20 |
| Resolved | 2026-09-20 |
| Route | luna |
| Milestone | ED-010-S6 |
| Kind | bug |
| Baseline commit | — |
| Motivation | The full S6 gate still observed an 18-tick acknowledgement spike because every tile-center change synchronously regenerated and sorted more than six thousand priorities. |
| Pre-existing context | The interest shape and priority are translation invariant for each of 16 quantized headings, so their relative order is immutable and safely reusable. |
| Acceptance | Moving the stream center reuses a pre-ranked circular offset table for the quantized heading instead of sorting the full 45-chunk interest set; ordering, wrapped boundaries, and sustained-flight acknowledgement tests pass. |
| Evidence | Height-tile interest shape, rotation, priority bands, heading jitter, and wrapped-boundary tests pass, including exact translation-invariant priority order across the seam. The 35-second sustained-flight acknowledgement test passed three consecutive fresh-process runs with the 16-tick contract unchanged. |
| Blocker | — |
| Owner | — |

## Plan

- Precompute ranked relative offsets once per quantized heading, translate them around each center, retain wrapped normalization, and verify ordering plus sustained flight.
- Precompute ranked relative offsets once per quantized heading, preserve relative priority order across wrapped centers, translate them around each center, and verify ordering plus sustained flight.

## Children

- None

## Product changes

- Fast flight no longer stalls authority when crossing terrain-tile boundaries.

## Code changes

- Cache ranked height-tile interest offsets by canonical heading and remove repeated full-window sorting.

## Resolution

- Changes: Precomputed ranked relative offsets once for each of the 16 canonical headings; center movement now translates cached offsets instead of sorting the full radius.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0228
