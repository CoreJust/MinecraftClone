# MC-AI-0231 — Remove cold-start terrain stalls and restore throughput

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0231 |
| Level | basic |
| Parent | MC-AI-0037 |
| Status | done |
| Finalized | true |
| Priority | P0 |
| Complexity | medium |
| Created | 2026-09-20 |
| Resolved | 2026-09-20 |
| Route | luna |
| Milestone | ED-010-S6 |
| Kind | bug |
| Baseline commit | — |
| Motivation | The r3 hosted macOS gate deterministically stalls at the first terrain-interest request: each isolated test process constructs and sorts all 16 direction orders on the authoritative server thread after join, producing a 42-input lag. Tick-only delivery also caps initial throughput below the established five-second contract. |
| Pre-existing context | Terrain interest topology is immutable process-wide and can be prepared before the server accepts players. One CPU remains dedicated to authority and transport; remaining cores may generate terrain up to the eight-worker cap. Control-first tick admission remains bounded by the advertised eight-batch credit window. |
| Acceptance | Cold-start terrain priority initialization cannot stall authoritative input after a player connects; initial progressive streaming delivers at least 1024 nearest-first tiles within five seconds on hosted macOS; the unchanged sustained-flight acknowledgement lag remains at most 16 inputs. |
| Evidence | Cold-start preview tests pass: 1024 nearest-first tiles arrive in about one second locally, the full preview suite passes, stalled clients remain bounded to the eight-batch window, and 35 seconds of 500x flight stays within the unchanged 16-input acknowledgement limit. Worker policy tests cover 0, 1, 2, 3, 4, 9, and 64 reported CPUs. |
| Blocker | — |
| Owner | — |

## Plan

- Prepare immutable terrain-interest orders during server construction before worker/network activity, use the full negotiated per-tick delivery window, and retain cold-start end-to-end regressions.
- Prepare immutable terrain-interest orders during server construction before worker/network activity, reserve one CPU for authority and transport, use the full negotiated per-tick delivery window, and retain cold-start end-to-end regressions.

## Children

- None

## Product changes

- Eliminate the cold-start movement freeze when terrain streaming begins and restore fast initial terrain fill.

## Code changes

- Add explicit terrain-interest preparation before server startup and admit up to the negotiated terrain delivery window per tick.
- Prepare terrain-interest orders before accepting players, reserve one CPU while using other cores for generation, and admit up to the negotiated terrain delivery window per tick.

## Resolution

- Changes: Prepared immutable terrain-interest ordering before gameplay ingress, restored two terrain workers on three-core hosts while retaining one control CPU, admitted the full negotiated window per tick, and sorted bounded delivery candidates only once per tick.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0231
