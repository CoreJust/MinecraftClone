# MC-AI-0238 — Repin CoreLang 0.1.2.2 and fairly dispatch terrain generation

Generated by `python3 script/ai_tasks.py render`; edit `backlog.json`, then rerender.

| Field | Value |
|---|---|
| ID | MC-AI-0238 |
| Level | basic |
| Parent | MC-AI-0037 |
| Status | done |
| Finalized | true |
| Priority | P0 |
| Complexity | medium |
| Created | 2026-09-20 |
| Resolved | 2026-09-20 |
| Route | luna |
| Milestone | ED-010-S6 |
| Kind | bug |
| Baseline commit | — |
| Motivation | Remove the final cross-platform S6 release blockers without weakening the terrain ordering or multiplayer streaming contracts. |
| Pre-existing context | Hosted macOS produced only 640 of an arbitrary 1024-tile shape sample within five seconds, while the sustained four-client case gave some clients no terrain because the global worker queue was filled by earlier streams. CoreLang 0.1.2.1 also failed MSVC Debug and is superseded by reviewed v0.1.2.2. |
| Acceptance | MinecraftClone pins immutable CoreLang 0.1.2.2 commit 62bd3857df428c115b3c804ab8c8c9387c1d7294; server terrain generation dispatch is fair across all four supported preview clients; the progressive priority test validates a sufficiently large deterministic prefix without using throughput as a shape oracle; focused and full release gates pass on macOS and native Windows. |
| Evidence | CoreLang v0.1.2.2 tag and release resolve to 62bd3857df428c115b3c804ab8c8c9387c1d7294 after macOS and native Windows Debug/Release 25/25 CTests. MinecraftClone Release built against that exact clean package. GameServerPreviewTest passed 9/9 in 72.91 seconds; the progressive and sustained four-client tests passed three additional consecutive iterations, with each progressive prefix completing in about one second and each four-client 500x run delivering terrain to every client. |
| Blocker | — |
| Owner | codex |

## Plan

- Repin the immutable CoreLang 0.1.2.2 release.
- Dispatch generation work round-robin across preview streams under the existing global worker budget.
- Separate priority-shape assertions from sustained throughput deadlines and verify all four clients make terrain progress.

## Children

- None

## Product changes

- Keep multiplayer terrain generation moving for every connected player while movement remains responsive.

## Code changes

- Fairly share generation dispatch, repin CoreLang, and harden hosted streaming regressions.

## Resolution

- Changes: Pinned reviewed CoreLang 0.1.2.2, rotated the server-wide generation dispatch budget fairly across preview streams, and changed the forward-priority regression to inspect a 512-tile prefix while retaining its five-second deadline.

## Commits

- No imported commits

Commit lookup: python3 script/ai_history.py show MC-AI-0238
